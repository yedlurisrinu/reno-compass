# Tier Matrix — reference pointer

The authoritative frozen Tier-1 trigger matrix and IRC/NEC rule base live in the `irc-safety` skill:
`../../irc-safety/SKILL.md`.

Read that skill's matrix when classifying:
- Structural: two-gate (slab suppresses; framed floor by filled-weight band × aggravating conditions).
- Electrical: amperage/circuit thresholds vs standard panel capacity; GFCI requirements (NEC 210.8).
- Gas load; ventilation/exhaust; wall modification.

This file is a pointer only (single source — the matrix is not duplicated here). The envelope shapes recorded
on each TierClassification are defined in the irc-safety skill (RD-1 §F).
