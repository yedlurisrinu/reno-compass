# Deterministic tools for consolidation-summary
- pdf_generator.py — render the family-facing plan PDF (safety-forward order).
- xlsx_generator.py — render the materials spreadsheet as a SEPARATE artifact (not referenced in PDF).
