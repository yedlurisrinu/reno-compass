services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - STORAGE_LOCAL_FALLBACK=false
      - MOCK_VERTEX_AI=false
      - PYTHONPATH=src
    volumes:
      - ./static:/app/static
