services:
  app:
    build: .
    ports:
      - "8020:8020"
    environment:
      - PORT=8020
      - STORAGE_LOCAL_FALLBACK=false
      - MOCK_VERTEX_AI=false
      - PYTHONPATH=src
    volumes:
      - ./static:/app/static
      - ./sources:/app/sources
